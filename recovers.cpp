#include<bits/stdc++.h>
using namespace std;


int main () {
    int t;
    cin>>t;
    while (t--) {
        int n;
        cin>>n;
        vector<int>arr(n);
        deque<int>d;
        int start=0;
        int end=n-1;
        int count=1;
        while (start<=end) {
            if (end==start) {
                d.push_front(arr[end]);
            }
            else if (count%2!=0){
                if ()
            }
        }
    }
}